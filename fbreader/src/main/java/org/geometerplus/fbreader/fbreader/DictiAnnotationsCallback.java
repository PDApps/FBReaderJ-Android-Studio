package org.geometerplus.fbreader.fbreader;

import org.geometerplus.android.fbreader.util.DBookmark;

import java.util.List;

public interface DictiAnnotationsCallback {
    List<DBookmark> getAnnotations();
}
